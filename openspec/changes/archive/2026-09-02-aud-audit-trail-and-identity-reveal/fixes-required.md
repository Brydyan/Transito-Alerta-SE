# Fixes Required: AUD — sc-327 (ronda 3)

Generated by `sdd-verify` on 2026-09-06 (round 3). **No CRITICAL remains.** Both
round-2 CRITICALs (FIX-5, FIX-6) are confirmed resolved by mutation testing — see
`verify-report.md` §0/§1. This change is clear to proceed to `sdd-archive`.

---

## Round-2 CRITICALs — status

- **FIX-5** (guard on `create()`/`update()`) — **CLOSED**. Both methods now call
  `assertRevealOnlyForMaster` before persisting. Confirmed by mutation: disabling the
  guard fails exactly 6 tests (4 pre-existing `syncPermissions` tests + 2 new
  `create`/`update` tests), 0 false negatives.
- **FIX-6** (rename-based bypass via seeded role names) — **CLOSED**. `update()` now
  evaluates the guard against the *resulting* name/permissions, and a new
  `assertSeededNameNotRenamed` rejects renaming to/from any of `master`, `admin_org`,
  `operador_org`, `operador_sistema`, `reporter`. Confirmed by mutation: disabling the
  rename guard fails exactly 4 tests, while the positive-case test (renaming two
  non-seeded names) correctly keeps passing.

---

## Recommended, non-blocking (carried forward, do not gate archive)

- **Cache-invalidation direct test** — `permission_version` bump on the `REVEAL
  incidents` grant (migration 0047) is still only asserted structurally (reading the
  migration SQL), not via a test that provisions a cached session and confirms the
  cache actually invalidates. Low priority — the bump-and-invalidate pattern is already
  covered generically elsewhere in the permission system.
- **`apply-progress.md` round-3 addendum** — the file still reads as if the change
  shipped clean in round 1. Recommend appending a short "Ronda 3" section noting
  FIX-5/FIX-6 before archiving, so the artifact trail is self-consistent. Documentation
  debt only, not a code defect.

Everything else named in round 1 and round 2 (`fixes-required.md` history: FIX-1
through FIX-6, WARNING-1 through WARNING-C) is now resolved and re-verified by
execution — see `verify-report.md` for the full evidence trail.
