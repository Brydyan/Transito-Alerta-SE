# Tasks: T3.8 Locations

Source: `sdd/t3.8-locations/spec` (obs #415), `sdd/t3.8-locations/design` (obs #416). Strict TDD: RED (failing test) then GREEN (implement) for every behavioral task.

## Phase 1: Schema (Migration + Entity)

- [x] 1.1 Create `database/migrations/0013_geo_zones_hierarchy.sql`: `ALTER TABLE geo_zones ADD COLUMN parent_id uuid NULL REFERENCES geo_zones(id) ON DELETE SET NULL`, `ADD COLUMN level varchar(20) NOT NULL DEFAULT 'zona'`; `chk_geo_zones_level` CHECK (level IN provincia/canton/parroquia/zona) and `chk_geo_zones_parent_not_self` CHECK (id <> parent_id), both wrapped in `DO $$ ... pg_constraint` guards (no `ADD CONSTRAINT IF NOT EXISTS` in PG); index on `parent_id`; guarded backfill UPDATEs by deterministic UUID literal only (`...000024`→provincia, `...000101/102/103`→canton w/ `parent_id='...000024'` gated by `EXISTS` on the province row — no name matching); insert 4 `('geo-zones', ACTION)` permission catalog rows (CREATE/READ/UPDATE/DELETE); wrap in `BEGIN;`/`COMMIT;`, idempotent
- [x] 1.2 Create `database/rollback/0013_geo_zones_hierarchy.DOWN.sql`: drop the two CHECK constraints, drop the `parent_id` index, drop columns `level` and `parent_id`, remove the 4 seeded `geo-zones` permission rows
- [x] 1.3 Modify `backend/src/entities/geo-zone.entity.ts`: add `@Column({ name: 'parent_id', type: 'uuid', nullable: true }) parentId!: string | null;` and `@Column({ type: 'varchar' }) level!: string;` — flat columns only, NO self-relation decorator; export `GEO_ZONE_LEVELS = ['provincia','canton','parroquia','zona'] as const` from this file as the single source of truth for DTO/service/migration. Note: `geo_zones` has NO `updated_at` column — 0013 does not add one, no downstream task may assume it exists.

## Phase 2: Geofencing Cache Additions (existing GeofencingService)

- [ ] 2.1 RED: in `backend/src/modules/geofencing/geofencing.service.spec.ts`, write failing tests for `tagPointCacheKey(cacheKey)` (SADD onto `POINT_CACHE_TAG_KEY = 'geo:tags:points'` via the raw ioredis client, DB 0) and `purgePointCache()` (SMEMBERS on DB 0 → `cache.del()` per member on DB 1 → `redis.del()` the tag-set on DB 0)
- [ ] 2.2 RED (regression guard): in the same spec, assert `tagPointCacheKey`/point-cache entries are NEVER added to or removed via `ALL_ZONES_TAG`/`purgeZoneCache(zoneId)` — an incident create/status-change purge (which calls `purgeZoneCache(ALL_ZONES_TAG)`) must NOT touch the point-cache tag-set, proving the two namespaces stay decoupled (this is the exact regression D-CACHE's rejected-option analysis warns against: `incidents.service.ts:151` purges `ALL_ZONES_TAG` on every incident write)
- [ ] 2.3 GREEN: implement `tagPointCacheKey` and `purgePointCache` in `backend/src/modules/geofencing/geofencing.service.ts`; export `POINT_CACHE_TAG_KEY` constant; call `tagPointCacheKey(key)` only inside the existing `if (zone !== null)` cold-path branch of `getCachedZoneByPoint` (after `this.cache.set(...)`, lines ~96-98). Note: `geo:tags:points` must NEVER be given an EXPIRE/TTL — it is fully truncated (`redis.del`) on every `purgePointCache()` call; a non-refreshed TTL would silently orphan sub-60s entries and reopen the staleness window the design closes.

## Phase 3: Repository (geo-zones module, raw SQL)

- [ ] 3.1 RED: create `backend/src/modules/geo-zones/geo-zones.repository.spec.ts` for the geometry write path — `ST_SetSRID(ST_GeomFromGeoJSON($1::text),4326)` then `ST_Multi(...)` coercion, and a pre-flight validity round trip returning `ST_IsValid`, `ST_IsValidReason`, `ST_IsEmpty`, `ST_GeometryType` (design D6, catches a Point silently promoted to MULTIPOINT)
- [ ] 3.2 GREEN: implement `backend/src/modules/geo-zones/geo-zones.repository.ts` write methods (`insert`, `update` with explicit `parent_id_provided` boolean param since COALESCE cannot distinguish "absent" from "null=detach to root"), plus reads (`findById`, `list` w/ filters, `ST_AsGeoJSON(polygon)::json` always returned as MultiPolygon) via `@InjectDataSource().query()`
- [ ] 3.3 RED: add tests for `getSubtree(rootId)` recursive CTE (depth cap 1000, sorted by name per level, includes inactive) and `validateNoCycles(zoneId, proposedParentId)` ancestor-walk (self-parent, direct A→B→A, transitive A→B→C→A)
- [ ] 3.4 GREEN: implement `getSubtree()` recursive CTE and `validateNoCycles()` ancestor walk in `geo-zones.repository.ts`, called inside the write transaction

## Phase 4: Service (business logic + error mapping + cache purge)

- [ ] 4.1 RED: create `backend/src/modules/geo-zones/geo-zones.service.spec.ts` — `create()`/`update()` validate `level` against `GEO_ZONE_LEVELS`, parent existence (400 PARENT_NOT_FOUND), parent-level-is-immediate-ancestor (400 INVALID_PARENT_LEVEL), cycle guard (400 CYCLIC_PARENT); geometry rejection maps to 400 INVALID_GEOMETRY / EMPTY_GEOMETRY / INVALID_GEOMETRY_FORMAT with `ST_IsValidReason` text; `delete()` sets `active=false` (never a real DELETE), idempotent (204 even if already inactive); missing id → 404; deactivating a parent does NOT cascade to children (test: children remain `active=true`)
- [ ] 4.2 RED (same spec): purge orchestration — `purgeGeoCaches()` called AFTER commit, in order `purgeZoneCache(zoneId)` → `purgeZoneCache(ALL_ZONES_TAG)` → `purgePointCache()`, and ONLY when `polygon` was supplied OR `active` actually flipped (design D8) — assert rename/level/parent_id-only updates do NOT trigger a purge
- [ ] 4.3 GREEN: implement `backend/src/modules/geo-zones/geo-zones.service.ts`: `create(dto)`, `update(id, dto)`, `delete(id)` (soft, sets active=false), `findById(id)` (200 even if inactive, 404 if missing), `list(filters)` (active-only default + `include_inactive`, search/parent_id/level/active filters, `{items, total}` shape), `getTree()` (delegates to repository, all zones incl. inactive), `purgeGeoCaches(zoneId)` orchestration per D8

## Phase 5: DTOs, Module, Controller

- [ ] 5.1 Create `backend/src/modules/geo-zones/dto/create-geo-zone.dto.ts`: `name` (required string), `level` (required, `@IsIn(GEO_ZONE_LEVELS)`), `parent_id` (optional uuid), `polygon` (required — custom class-validator constraint on plain `object`, NOT `@ValidateNested`, per design D7 to avoid whitelist stripping `coordinates`), `active` (optional bool, default true)
- [ ] 5.2 Create `backend/src/modules/geo-zones/dto/update-geo-zone.dto.ts`: same fields, all optional, `parent_id` explicitly nullable to support detach-to-root
- [ ] 5.3 Create `backend/src/modules/geo-zones/geo-zones.module.ts`: providers `GeoZonesRepository`, `GeoZonesService`, `GeoZonesController`; import `GeofencingModule` (already exports `GeofencingService` — no change needed there)
- [ ] 5.4 Modify `backend/src/modules/geofencing/geofencing.module.ts`: reword docblock line 9 from "other modules (Incidents) inject GeofencingService rather than querying geo_zones directly" to reflect that GeoZonesService now owns direct writes to `geo_zones` — e.g. "owns geo_zones reads; GeoZonesService (admin CRUD) owns writes". Comment-only change — `exports: [GeofencingService]` at line 13 stays as-is.
- [ ] 5.5 Modify `backend/src/modules/incidents/incidents.service.ts`: no functional change — confirm `ALL_ZONES_TAG` (line 20) stays defined here per design D10 (re-exported, not moved, since D10 says move to `geofencing.service.ts` and re-export — verify against current design text before touching; if moving, keep the 2-line re-export so `incidents.service.ts` callers are unaffected)
- [ ] 5.6 RED: create `backend/src/modules/geo-zones/geo-zones.controller.spec.ts` — routes return entities directly, NO `{data}` envelope, list returns `{items, total}`; route order `GET /tree` and other static routes declared BEFORE `GET /:id`; `@UseGuards(JwtAuthGuard, PermissionGuard)` class-level; `@RequirePermission('CREATE'|'UPDATE'|'DELETE')` per verb, resource `geo-zones` inferred from path
- [ ] 5.7 GREEN: implement `backend/src/modules/geo-zones/geo-zones.controller.ts` at `@Controller('geo-zones')`: `GET /tree` (first), `GET /` (paginated, filtered), `GET /:id` (`ParseUUIDPipe`, 200 incl. inactive, 404 if missing), `POST` (`@RequirePermission('CREATE')`, 201), `PATCH /:id` (`@RequirePermission('UPDATE')`), `DELETE /:id` (`@RequirePermission('DELETE')`, `@HttpCode(204)`, idempotent soft-delete)
- [ ] 5.8 Modify `backend/src/app.module.ts`: register `GeoZonesModule`

## Phase 6: E2E Integration

- [ ] 6.1 Create `backend/test/e2e/geo-zones.e2e-spec.ts` covering all 13 spec scenarios:
  - TS-1 create root provincia zone; TS-2 create child canton w/ `parent_id`; TS-3 bare GeoJSON Polygon coerced to MultiPolygon on read; TS-4 self-intersecting bowtie → 400 INVALID_GEOMETRY; TS-5 invalid `level` → 400; TS-6 `parent_id` not found → 400 PARENT_NOT_FOUND; TS-7 cycle on re-parent → 400 CYCLIC_PARENT; TS-8 `GET /tree` depth≥2 (Santa Elena + 3 cantones); TS-9 deactivate parent → children stay active; TS-10 inactive zone excluded from geofencing containment; TS-11 (CC5) shrink boundary → only NEW incidents after the edit resolve outside the zone, existing incidents' `zone_id` unchanged, asserted deterministically via `env.redisCache.exists('geo:point:<lat3>:<lng3>') === 0` right after PATCH (DB 1) — no sleep; TS-12 permission guards → 403 on POST/PATCH/DELETE without the matching permission; TS-13 seed backfill verification — after migration 0013, seeded province row has `level='provincia'` AND `parent_id IS NULL`, its 3 seeded cantones have `level='canton'` AND `parent_id` = the province's id
  - Constraint: `test-environment.ts` `reset()` PRESERVES `geo_zones` seed rows — every scenario that mutates a boundary or deactivates/creates a zone (TS-3, TS-9, TS-10, TS-11) MUST create a throwaway zone via `env.pg.query` OUTSIDE the seeded Santa Elena bbox (`lng[-81.008,-80.200] × lat[-2.508,-1.669]`, e.g. lat 0.500/lng -78.000) and hard-delete it in `afterEach` — `findZoneByPoint` is `LIMIT 1` with no `ORDER BY`, so a leftover overlapping zone would nondeterministically affect other suites depending on run order

## Implementation Order

Sequential, each phase gates the next: 1.1-1.3 (schema/entity) → 2.1-2.3 (geofencing cache additions, TDD) → 3.1-3.4 (repository, TDD) → 4.1-4.3 (service, TDD) → 5.1-5.8 (DTOs/module/controller, TDD for controller) → 6.1 (e2e, all layers wired). Total 25 tasks. Phase 2 is independent of Phase 1 (no schema dependency) and could run in parallel with 1.1-1.3, but is sequenced after for review simplicity; Phases 3-6 are strictly sequential.
