# Tasks — migracion_framework_angular

- [x] T1 — Configurar y verificar la arquitectura de arranque standalone en `main.ts` y `app.config.ts`. Cubre: R1, R3.
- [x] T2 — Definir el componente principal `App` y componentes adicionales asegurando la propiedad standalone sin uso de `NgModule`. Cubre: R2, R3.
- [x] T3 — Implementar servicio del sistema `AlertSystemService` utilizando la inyección de dependencias nativa (`@Injectable({ providedIn: 'root' })`). Cubre: R4.
- [x] T4 — Crear e implementar pruebas unitarias en `app.spec.ts` para verificar compilación, instanciación del componente y resolución de dependencias DI. Cubre: R1, R5.
